1. 자바에서 JVM(Java Virtual Machine)에 의해서 최초로 호출 되어지는 메서드를 작성하시오 [10점]
--------------------------------------------------------
답 : main 메서드
--------------------------------------------------------

2. 다음 프로그램 중 3번째 줄이 오류없이 실행될 수 있는 이유를 서술하시오. [10점]
  - int i = 100;
  - double d;
  - d = i;
--------------------------------------------------------
답 : 정수형 변수 i 는 실수형 d로 자동형변환이 가능하다.
--------------------------------------------------------

3. 다음 비교문인 if 문에서 값이 true가 될 때 실행되도록 프로그램을 완성하고 이유를 서술하시오. [10점]

String name1 = new String ( "JAVA" );
String name2 = new String ( "JAVA" );
if ( _______ ) System.out.println( "값이 같습니다.");
--------------------------------------------------------
답 : name1.equals(name2)
--------------------------------------------------------

4. 다음 프로그램을 실행하면 컴파일 에러가 발생한다.
   에러가 발생한 라인과 그 이유에 대해 기술하시오. [10점]
---------------------------------------------------------
1	public static void main(String[] args) {
2		int x = 5;
3		int y = 7;
4		if(x > 20) {
5			int z = ++x + y;
6		}
7		System.out.println(z);
8	}
--------------------------------------------------------
답 : if 문 안에서 지역변수라서 if 문 밖에서는 사용할 수 없다.
--------------------------------------------------------

5. Math.random() 메소드를 이용하여 1에서 10사이의 정수 난수 발생시키는 프로그램을 작성하세요.[10점]
--------------------------------------------------------
답 )    public static void main(String[] args) {
		
		for(int i = 1; i <= 10; i++){
			double rand = Math.random() * 10;
			int n = (int) rand + 1;
			
			System.out.println(n);
		}
	}
--------------------------------------------------------

6. 접근제한자에 대해 설명하시오
--------------------------------------------------------
답)
private		같은 클래스 사용가능
default		같은 패키지 사용가능
protected	상속받은 클래스에서 사용가능
public		어디서나 사용가능

--------------------------------------------------------
7. 1~10까지 1씩 증가 시키면서 출력하는 반복문 
--------------------------------------------------------
답)

for (int i=1 ; i<=10; i++){
 System.out.println(i);
}

--------------------------------------------------------

8. 생성자에 대하여 서술하시오
--------------------------------------------------------

return없는 유일한 메소드

생성자 이름 = 클래스 이름
매개변수 종류에 따라 여러개 생성 가능
  >> 오버로딩 가능하다
  >> 매개변수 순서, 종류, 갯수 등 달라야 한다.
객체 초기화
생성자 없으면 내부적으로 디폴트 생성자 가진다.
  >> 인자있는 생성자 만들면 기본생성자 X (직접 만들어줘야..)

--------------------------------------------------------
9. 다음 ArrayList의 값을 출력하되 단축for문으로 출력하는 프로그램을 완성하시오.[10점]

List<String> list = new ArrayList<String>()// 객체생성
list.add("피자");
list.add("치킨");
list.add("사이다");
list.add("신라면");
list.add("콜라");

for(   ①   ){
    System.out.println(   ②   );
--------------------------------------------------------
답:① String s:list
   ② s

List<String> list = new ArrayList<String>();
	list.add("피자");
	list.add("치킨");
	list.add("사이다");
	list.add("신라면");
	list.add("콜라");
	
	for(String s:list){
		System.out.println(s);
	}
--------------------------------------------------------
10. 나눠지는 숫자 int num 이  0일때
    ArithmeticException 발생한다.
    숫자 입력받아 100을 입력받은 숫자로 나누고, 결과를 출력하는 작업..
    예외 발생하면 예외처리 하는곳으로 이동하라는 프로그램 완성하시오

int num = scanner.nextInt();

(    1    ){

int result = 100/num;
System.out.println(result);

} () (){
  System.out.println("0으로 나누기 오류입니다.");
}

--------------------------------------------------------

  int num = scanner.nextInt();

    // ( 1 ){
        try {

            int result = 100 / num;
            System.out.println(result);

     // } ( 2 )   ( 3 )                  {
        } catch (ArithmeticException ae) {

            System.out.println("0으로 나누기 오류입니다.");
        }

--------------------------------------------------------




