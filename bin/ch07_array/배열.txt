배열(array)

배열은 동질자료형  집합체

int a;
int b;
int c;

int d[] = new int[3];
 >>> 3칸짜리 int 배열 생성

d[0] = a;
d[1] = b;
d[2] = c;

d[] = {a, b, c};

자료 일괄처리 가능
new 연산자 이용해서 생성

heap 영역에 동적 메모리 할당
new ### >>  heap 영역에 클래스 객체를 생성하는 연산자
            java에서는 배열도 클래스의 일종






