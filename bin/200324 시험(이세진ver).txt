1. 자바에서 JVM(Java Virtual Machine)에 의해서 최초로 호출 되어지는 메서드를 작성하시오 [10점]
--------------------------------------------------------
답 : main 메서드
--------------------------------------------------------

2. 다음 프로그램 중 3번째 줄이 오류없이 실행될 수 있는 이유를 서술하시오. [10점]
  - int i = 100;
  - double d;
  - d = i;
--------------------------------------------------------
답 : 정수형 변수 i 는 실수형 d로 자동형변환이 가능하다.
--------------------------------------------------------

3. 다음 비교문인 if 문에서 값이 true가 될 때 실행되도록 프로그램을 완성하고 이유를 서술하시오. [10점]

String name1 = new String ( "JAVA" );
String name2 = new String ( "JAVA" );
if ( _______ ) System.out.println( "값이 같습니다.");
--------------------------------------------------------
답 : name1.equals(name2)
--------------------------------------------------------

4. 다음 프로그램을 실행하면 컴파일 에러가 발생한다.
   에러가 발생한 라인과 그 이유에 대해 기술하시오. [10점]
---------------------------------------------------------
1	public static void main(String[] args) {
2		int x = 5;
3		int y = 7;
4		if(x > 20) {
5			int z = ++x + y;
6		}
7		System.out.println(z);
8	}
--------------------------------------------------------
답 : if 문 안에서 지역변수라서 if 문 밖에서는 사용할 수 없다.
--------------------------------------------------------

5. Math.random() 메소드를 이용하여 1에서 10사이의 정수 난수 발생시키는 프로그램을 작성하세요.[10점]
--------------------------------------------------------
답 )    public static void main(String[] args) {
		
		for(int i = 1; i <= 10; i++){
			double rand = Math.random() * 10;
			int N = (int) rand + 1;
			
			System.out.println(N);
		}
	}
--------------------------------------------------------
6. 다음 접근 제한자에 대한 설명을 쓰시오 [10점]
   1) private
   2) 생략(default)
   3) protected
   4) public
--------------------------------------------------------
답 : 1)같은 클래스에서만 사용가능
     2)같은 패키지에서만 사용가능
     3)모든 패키지내의 클래스를 상속 받으면 사용가능
     4)모든 패키지내의 클래스를 어느 클래스에서나 사용 가능
--------------------------------------------------------
7. 1부터 10까지 1씩 증가시키면서 출력하는 반복문(for)을 출력하시오.[10점]
--------------------------------------------------------
답: public static void main(String args[]){
	for(int i=1; i<=10; i++){
		System.out.println(i);
	}
    }
--------------------------------------------------------
8. 생성자에 대하여 서술하시오.
--------------------------------------------------------
답: 1. 클래스명과 같은 이름을 써야한다.
    2. 리턴값이 없다. 객체( 필드 ) 초기화를 한다.
    3. 인수(매개변수)를 갖는다. 오버로드 가능하다
    4. 객체 생성시 생성자 호출 ex) Test t=new Test();
    5. 생성자를 기재하지 않으면 내부적으로 디폴트 생성자가 만들어진다.
    6. 인자있는 생성자가 있을때는 디폴트 생성자가 생성되지 않는다.
--------------------------------------------------------
9. 다음 ArrayList의 값을 출력하되 단축for문으로 출력하는 프로그램을 완성하시오.[10점]

List<String> list = new ArrayList<String>()// 객체생성
list.add("피자");
list.add("치킨");
list.add("사이다");
list.add("신라면");
list.add("콜라");

for(   ①   ){
    System.out.println(   ②   );
--------------------------------------------------------
답:① String s:list
   ② s

List<String> list = new ArrayList<String>();
	list.add("피자");
	list.add("치킨");
	list.add("사이다");
	list.add("신라면");
	list.add("콜라");
	
	for(String s:list){
		System.out.println(s);
	}
--------------------------------------------------------
10. 다음과 같은 프로그램에서 나눠지는 숫자(num)값이 0이 되면 ArithmeticException이
    생기는것을 알게되었다. 그래서 숫자를 입력받아 100을 그 숫자로 나누고 결과를 출력하는
    일련의 작업에서 나누기 처리를 할 때 예외가 발생이 되면 "0으로 나누기 오류입니다."라고
    출력하는 프로그램을 완성하시오.

int num = scanner.nextInt();
(   ①   ){
   int resilt = 100 / num;
   System.out.println(result);
}(   ②   )(   ③   ){
   System.out.println( "0으로 나누기 오류입니다." );
}
--------------------------------------------------------
답: ①try
    ②catch
    ③Exception ex

import java.util.Scanner;

public class ex {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int num = sc.nextInt();
		try{
		   int result = 100 / num;
		   System.out.println(result);
		}catch(Exception ex){
		   System.out.println( "0으로 나누기 오류입니다." );
		}
	}
}
--------------------------------------------------------







